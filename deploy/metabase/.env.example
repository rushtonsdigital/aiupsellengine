# Copy to .env (same folder) before `docker compose up -d`.
# This password is for Metabase's OWN metadata Postgres container only — it is
# unrelated to Supabase or the rushtons_readonly role. Pick anything strong;
# you never type it again after first boot.
METABASE_APP_DB_PASSWORD=change-me-to-something-strong
